# HNPF-MCP 打磨路线（2026-08-15 规划）

> 项目当前状态：**37 个 MCP 工具 + 7 ke.* 连接器 + 4 Prompts + 2 资源**，三层抽象真机闭环，real-all 16/0，支持多开与主菜单自动进扩展。
> 本文档列出后续打磨方向，按价值/成本分批执行。✅ = 已完成。

## A. 功能补全

| # | 项 | 现状 | 方案 | 优先级 |
|---|---|---|---|---|
| A1 | ✅ **任务详情解析** | `get_mission` 只有标题+完成数（MisisonGoal 无 Description） | `mission.detail`：任务标题 / goals 类型名+完成状态 / `reloadGoalsSourceFile` 指向的 XML 全文（File.ReadAllText），AI 自己理解目标参数 | ⭐P0 |
| A2 | ✅ **存档快照** | `save_game` 覆盖式存档 | save 前自动复制存档做时间戳快照（bridge 侧文件操作），AI 试错可回退 | ⭐P0 |
| A3 | ✅ **hackscript 成功路径** | 原版脚本有破坏性（forkbomb/delete），只验证了报错路径 | 造无害测试脚本（writel/connect/delay）放测试扩展验证成功路径 | P1 |
| A4 | ✅ **IRC/论坛读取** | 只能 run_action 发不能读 | `irc.read`（IRCDaemon.System.GetLogsFromFile 反射，Name/Message/Time）+ `board.read`（MessageBoardDaemon threads 文件夹）——均为 37 个工具之一 | P1 |
| A5 | ✅ **registry 补全** | actions 有参数反射；exe/daemon 只有名字 | exe 反射构造参数、daemon 反射公开字段 | P1 |
| A6 | **防火墙直改** | 只能读 firewallSolved | `firewall.solved = true` 写操作（调试场景） | P2 |
| A7 | **事件源扩展** | 5 类事件 | 任务目标完成、文件树 diff、节点状态轮询 | P2 |

## B. 性能/稳定性

| # | 项 | 现状 | 方案 | 优先级 |
|---|---|---|---|---|
| B1 | ✅ **命令同步输出** | `execute_command` 异步提交，AI 要手动查 history | server 侧 `waitMs` 参数：提交后延迟抓 terminal_history 一并返回（server 异步 sleep，不动 bridge 主线程） | ⭐P0 |
| B2 | ✅ **断线重连完善** | server 有重连，bridge 崩溃事件丢失 | 事件缓冲持久化/补发 | P1 |
| B3 | ✅ **多开游戏** | cfg 支持 pipe 名按 pid | bridge 管道自动改名（占用→-{pid}）+ server pipe_probe 探测 + HNPF_PIPE 指定 | P1 |
| B4 | **大响应分页** | get_network_map 全量 | 字段裁剪/分页 | P2 |
| B5 | **MCP notification 推送** | 事件靠拉取 | 可选标准推送 | P2 |
| B6 | **写操作限流** | 泵预算固定 8/帧 | 写操作单独预算 | P2 |
| B7 | **掉帧自适应** | 预算固定 | 卡顿时动态降预算 | P2 |

## C. 架构重构

| # | 项 | 现状 | 方案 | 优先级 |
|---|---|---|---|---|
| C1 | ✅ **Executor.cs 拆分** | 单文件 700+ 行 | partial class 按域拆 State/FileSystem/Command/Network | ⭐P0 |
| C2 | ✅ **通用 L3 连接器** | connector 硬引用 KE | 改名 HnpfMcpConnector：零模组编译引用（KeReflect 反射软依赖）+ 无 BepInDependency，单 DLL 服务多模组，模组缺失返回"未加载" | ⭐P0 |
| C3 | ✅ **协议版本化** | 无版本头 | 握手带版本，防 server-bridge 不匹配 | P1 |
| C4 | ✅ **配置热加载** | 启动读一次 | 白名单即时生效 | P2 |
| C5 | **统一响应格式** | 结构散 | {ok, data, error} 规范化 | P2 |

## D. 安全/运维

| # | 项 | 现状 | 方案 | 优先级 |
|---|---|---|---|---|
| D1 | ✅ **白名单正则化** | 精确命令名匹配 | 前缀/通配（scp /home/*） | ⭐P0 |
| D2 | ✅ **审计日志** | 无 | MCP 调用记录（方法/参数摘要/时间） | P1 |
| D3 | ✅ **一键发布脚本** | 手动 copy+重启 | build+copy+restart 脚本 | P1 |
| D4 | **只读细化** | 全局开关 | 按域只读（文件/命令/Flag） | P2 |
| D5 | ✅ **token 强化** | 默认空 | 默认随机 token | P2 |

## E. 文档/体验

| # | 项 | 现状 | 方案 | 优先级 |
|---|---|---|---|---|
| E1 | ✅ 使用指南补全 | 含 HN 命令速查引用 | P1 |
| E2 | ✅ **宿主接入教程** | 无具体配置 | WorkBuddy/Claude Desktop mcpServers 配置 | ⭐P0 |
| E3 | ✅ 架构图/时序图 | mermaid 三层抽象+拓扑+调用链 | P2 |
| E4 | ✅ 故障排查扩充 | 基础表 | 缺桥警告、类型 identity、Location 空、bridgeVersion、save_list PF 路径等 6 新条目 | P1 |

## F. 测试

| # | 项 | 现状 | 方案 | 优先级 |
|---|---|---|---|---|
| F1 | ✅ 真机自动化增强 | real-all 16 项 | mail 成功路径断言 + registry 描述提示 | P1 |
| F2 | 长稳测试 | 无 | 挂机 2h 事件流不丢 | P2 |
| F3 | 多版本兼容 | 只测 5.069 | Pathfinder 多版本 smoke | P2 |

## 执行批次

- **第一批** ✅（2026-08-15）：B1 命令同步输出 → E2 宿主接入教程 → C1 Executor 拆分 → A1 任务详情
- **第二批** ✅（2026-08-16）：文件夹重构（HNPF-MCP 大文件夹 + C# 扁平化）/ 连接器检测增强（HacknetChainloader + 主动 Scan）/ D1 白名单正则化 / D2 审计日志 / A2 存档快照（server 侧快照到 HNPF-MCP/snapshots/，保留 20 份，优先当前会话存档）/ C2 通用 L3 连接器（改名 HnpfMcpConnector + KeReflect 反射软依赖 + 无 BepInDependency）
- **8-16/8-17 专项** ✅：hackscript 成功路径（A3）/ 参数语义（XmlDoc 读模组 XML 文档注释，FlashScreen.Color 等 description）/ registry 补全（exe/daemon 参数）/ C3 协议版本化 / D3 deploy.ps1 一键发布 / C4 配置热加载 / D5 token 强化（AutoToken 开关）/ B2 断线日志 / E4 故障排查扩充 / F1 real-all 断言 / **launch_game + MenuExecutor 主菜单进扩展**（模拟 PF 插件确认 approvedInfo + ActivateExtensionPage → 插件真正加载；menu_enter_extension 新账号 / menu_load_extension_save 恢复存档——均真机闭环；Patcher 公开化字段 → 反射 Public|NonPublic 都加）/ save_list current 标记兼容路径型 SaveGameUserName
- **8-17 晚** ✅：**launch_game 弃用 -extstart**（改为正常启动 + 后台自动主菜单进扩展 scheduleAutoEnterExtension；spawn stdio 给管道防 Console 无句柄崩；**KE PrintGradientAscii 包 try/catch**——无控制台环境 Console.OutputEncoding 抛 IOException 导致插件加载失败）——另一宿主按旧参数调 launch_game{ext} 也能走正确路径，真机验证 KE 注册完整
- **8-17 傍晚** ✅：A4 IRC/论坛读取（irc.read / board.read，Executor.Messaging.cs，反射 IRCSystem.GetLogsFromFile + MessageBoard threads，BuildVersion 2026-08-17-1620，工具 37）
- **8-17 下午** ✅：B3 多开（bridge 管道自动改名 -{pid} + server pipe_probe + HNPF_PIPE）/ **HN 命令速查**（源码 Helpfile/ProgramList 提取 26 内置命令 + 6 exe + Pathfinder 自定义，HN vs Unix 差异文档 docs/HN命令速查.md + MCP prompt hn-command-guide 注册）/ E3 架构图（docs/架构图.md mermaid）/ save_list current 兼容路径型 SaveGameUserName（BuildVersion 2026-08-17-1318）
- **第三批**：其余 P1/P2 按需挑选
